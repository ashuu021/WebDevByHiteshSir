// multiply a array with 2 and store it in another array

let multiplyArray =[];
let array = [1,2,3,4];
for(let i =0;i<array.length;i++){
    multiplyArray.push(array[i]*2);
}
console.log(multiplyArray);
